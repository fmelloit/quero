<%= form_for(novinha) do |f| %>
  <% if novinha.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(novinha.errors.count, "error") %> prohibited this novinha from being saved:</h2>

      <ul>
      <% novinha.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :data_picote %>
    <%= f.date_select :data_picote %>
  </div>

  <div class="field">
    <%= f.label :nota %>
    <%= f.text_field :nota %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
